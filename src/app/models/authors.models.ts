// Interface for the authors creation
export interface IAuthorCreation{
    firstName: string,
    lastName: string;
}

// Interface for the authors reading
export interface IAuthors{
    id:number;
    firstName: string;
    lastName: string;
}